---
import Layout from "../../layouts/Layout.astro";
import imagehero from "../../assets/img/header/apropos.webp";
import './src/styles/global.css'

import { getOneInvite } from "../../../backend/backend.mjs";
const {id} = Astro.params;
const invite = await getOneInvite(id);
if (!invite) {
  return Astro.redirect('/invite')
}

console.log(invite);
---
<Layout src={imagehero} alt="" texte="Nos invités">
    <h1 class="mt-20 p-5">{invite.prenom} {invite.nom} </h1>
    <h2 class=" p-5">{invite.role}</h2>
    <p class=" p-5">{invite.biographie}</p>
    <img class="p-5" src={invite.img} alt="">
</Layout>